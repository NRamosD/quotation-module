function removeAlertUpload() {
    let deleteDiv = document.getElementById("alert-succes");
    deleteDiv.parentNode.removeChild(deleteDiv);
}
function removeAlertUploadProblem() {
    let deleteDiv = document.getElementById("alert-problem");
    deleteDiv.parentNode.removeChild(deleteDiv);
}